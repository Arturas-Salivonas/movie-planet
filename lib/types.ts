/**
 * Shared types for Next.js app
 * Re-exports from src/types.ts for use in both server and client components
 */

export type {
  Movie,
  Location,
  GeoPoint,
  Actor,
  FilterState,
} from '../src/types'
